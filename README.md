// Navigate between pages function goToPage(pageNum) {     const page = document.getElementById(`page${pageNum}`);     if (page) {         page.scrollIntoView({ behavior: 'smooth', block: 'start' });     } }  // Page 1: Envelope opening animation document.addEventListener('DOMContentLoaded', function() {     const tapBtn = document.getElementById('tapBtn');     const envelope = document.getElementById('envelope');     const letterContent = document.getElementById('letterContent');     const musicBtn = document.getElementById('musicBtn');     const petals = document.querySelectorAll('.petal');      if (tapBtn) {         tapBtn.addEventListener('click', function() {             envelope.classList.add('opened');                          // Animate petals             petals.forEach((petal, index) => {                 petal.style.setProperty('--tx', (Math.random() - 0.5) * 200 + 'px');                 petal.style.animationDelay = (index * 0.15) + 's';             });                          // Hide letter content initially             letterContent.style.opacity = '0';             letterContent.style.pointerEvents = 'none';                          // Show music button             setTimeout(() => {                 musicBtn.style.display = 'flex';                 letterContent.style.display = 'none';             }, 800);         });     }      // Music button functionality     if (musicBtn) {         musicBtn.addEventListener('click', function() {             // Play background music or toggle             console.log('Music toggled');             // You can integrate actual audio here         });     }      // Smooth scroll snap behavior     const container = document.querySelector('.scrapbook-container');     if (container) {         // Keyboard navigation         document.addEventListener('keydown', function(e) {             if (e.key === 'ArrowDown') {                 container.scrollBy({ top: window.innerHeight, behavior: 'smooth' });             } else if (e.key === 'ArrowUp') {                 container.scrollBy({ top: -window.innerHeight, behavior: 'smooth' });             }         });     }      // Parallax effect for story page     const storyImage = document.querySelector('.story-image');     if (storyImage) {         window.addEventListener('scroll', function() {             const scrollY = window.scrollY;             const page2 = document.getElementById('page2');             const page2Top = page2.offsetTop;                          if (scrollY >= page2Top && scrollY < page2Top + window.innerHeight) {                 const offset = (scrollY - page2Top) * 0.5;                 storyImage.style.transform = `translateY(${offset}px) scale(1.1)`;             }         });     }      // Intersection Observer for fade-in animations     const observerOptions = {         threshold: 0.1,         rootMargin: '0px'     };      const observer = new IntersectionObserver(function(entries) {         entries.forEach(entry => {             if (entry.isIntersecting) {                 entry.target.style.opacity = '1';                 entry.target.style.pointerEvents = 'auto';             }         });     }, observerOptions);      // Observe all pages     document.querySelectorAll('.page').forEach(page => {         observer.observe(page);     }); });  // Add touch swipe navigation for mobile document.addEventListener('touchstart', handleTouchStart, false); document.addEventListener('touchend', handleTouchEnd, false);  let xDown = null; let yDown = null;  function handleTouchStart(e) {     xDown = e.touches[0].clientX;     yDown = e.touches[0].clientY; }  function handleTouchEnd(e) {     if (!xDown || !yDown) return;          let xUp = e.changedTouches[0].clientX;     let yUp = e.changedTouches[0].clientY;          let xDiff = xDown - xUp;     let yDiff = yDown - yUp;          if (Math.abs(yDiff) > Math.abs(xDiff)) {         if (yDiff > 0) {             // Swiped up             document.querySelector('.scrapbook-container').scrollBy({                  top: window.innerHeight,                  behavior: 'smooth'              });         } else {             // Swiped down             document.querySelector('.scrapbook-container').scrollBy({                  top: -window.innerHeight,                  behavior: 'smooth'              });         }     }          xDown = null;     yDown = nul
